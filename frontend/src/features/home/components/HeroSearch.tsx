import { Input } from "@/components/ui/input";

const HeroSearch = () => {
    return (
        <div className="mt-8 max-w-xl">
            <Input placeholder="جستجوی مکان‌های تاریخی..." />
        </div>
    );
}

export default HeroSearch;